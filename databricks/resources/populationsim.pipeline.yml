resources:
  pipelines:
    populationsim_pipeline:
      name: Population-Sim-Pipeline
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      channel: CURRENT
      root_path: ..
      libraries:
        - glob:
            include: ../transformations/**
      configuration:
        popsim.source_root: ${var.source_root}
